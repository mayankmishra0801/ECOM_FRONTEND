import { CheckwishlistPipe } from './checkwishlist.pipe';

describe('CheckwishlistPipe', () => {
  it('create an instance', () => {
    const pipe = new CheckwishlistPipe();
    expect(pipe).toBeTruthy();
  });
});
